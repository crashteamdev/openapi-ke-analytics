name: Build Maven Artifact

on:
  pull_request:
    branches:
      - '*'

jobs:
  build:
    uses: crashteamdev/jvm-workflows/.github/workflows/maven-swag-build.yml@v1
    with:
      java-version: '21'
